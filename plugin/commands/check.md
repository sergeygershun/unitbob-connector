---
description: Run the guardrail suite and report which subsystems are green or red.
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/unitbob/workflows/check.md` and follow it exactly.

That file is the single copy of this workflow: the skill follows it too, so a
user who asks in plain words and a user who types this command get the same run.
