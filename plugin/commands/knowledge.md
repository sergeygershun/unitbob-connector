---
description: Talk a recorded feature through — Claude asks what it still needs to know, in your product's language, and writes down what "done" means.
argument-hint: <which feature, in your words>
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/unitbob/workflows/knowledge.md` and follow it
exactly, with `$ARGUMENTS` as the words that name the feature.

That file is the single copy of this workflow: the skill follows it too, so a
user who asks in plain words and a user who types this command get the same run.
