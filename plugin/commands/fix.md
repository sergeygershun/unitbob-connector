---
description: Act on a red guardrail — fix the code, or accept a deliberate behaviour change.
argument-hint: <interface_id>
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/unitbob/workflows/fix.md` and follow it
exactly, with `$ARGUMENTS` as the `<guard_id>` it refers to.

That file is the single copy of this workflow: the skill follows it too, so a
user who asks in plain words and a user who types this command get the same run.
