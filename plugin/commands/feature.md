---
description: Record what you are about to build, and see which product capabilities it may touch — before changing anything.
argument-hint: <what you want to build or change>
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/unitbob/workflows/feature.md` and follow it
exactly, with `$ARGUMENTS` as the `<intent>` it refers to.

That file is the single copy of this workflow: the skill follows it too, so a
user who asks in plain words and a user who types this command get the same run.
